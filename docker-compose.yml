services:
  qwen35-35b:
    image: vllm/vllm-openai:cu130-nightly
    container_name: qwen35-35b
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
    ports:
      - "0.0.0.0:8002:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: >
      Qwen/Qwen3.5-35B-A3B
      --served-model-name qwen3.5-35b
      --port 8000
      --host 0.0.0.0
      --max-model-len 262144
      --gpu-memory-utilization 0.80
      --reasoning-parser qwen3
      --enable-auto-tool-choice
      --tool-call-parser qwen3_coder
      --enable-prefix-caching
    restart: unless-stopped

  qwen35-122b:
    image: vllm/vllm-openai:cu130-nightly
    container_name: qwen35-122b
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
    ports:
      - "0.0.0.0:8003:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: >
      Qwen/Qwen3.5-122B-A10B
      --served-model-name qwen3.5-122b
      --port 8000
      --host 0.0.0.0
      --max-model-len 32768
      --max-num-batched-tokens 4096
      --gpu-memory-utilization 0.65
      --reasoning-parser qwen3
      --enable-auto-tool-choice
      --tool-call-parser qwen3_coder
      --enable-prefix-caching
    restart: unless-stopped
    profiles:
      - large

  deepseek-v32-speciale:
    image: vllm/vllm-openai:cu130-nightly
    container_name: deepseek-v32-speciale
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
    ports:
      - "0.0.0.0:8004:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: >
      deepseek-ai/DeepSeek-V3.2-Speciale
      --served-model-name deepseek-v32-speciale
      --port 8000
      --host 0.0.0.0
      --max-model-len 32768
      --gpu-memory-utilization 0.75
      --enable-prefix-caching
      --trust-remote-code
    restart: unless-stopped

  kimi-k25:
    image: vllm/vllm-openai:cu130-nightly
    container_name: kimi-k25
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
    ports:
      - "0.0.0.0:8005:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: >
      moonshotai/Kimi-K2.5
      --served-model-name kimi-k25
      --port 8000
      --host 0.0.0.0
      --max-model-len 32768
      --gpu-memory-utilization 0.75
      --enable-prefix-caching
      --trust-remote-code
    restart: unless-stopped

  mimo-v2-flash:
    image: vllm/vllm-openai:cu130-nightly
    container_name: mimo-v2-flash
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
    ports:
      - "0.0.0.0:8006:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: >
      XiaomiMiMo/MiMo-V2-Flash
      --served-model-name mimo-v2-flash
      --port 8000
      --host 0.0.0.0
      --max-model-len 65536
      --max-num-batched-tokens 32768
      --gpu-memory-utilization 0.90
      --trust-remote-code
      --generation-config vllm
      --tool-call-parser qwen3_xml
      --reasoning-parser qwen3
    restart: unless-stopped
