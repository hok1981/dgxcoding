services:
  qwen3-a3b:
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: qwen3-a3b
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=nvidia/Qwen3-30B-A3B-NVFP4
      - MAX_BATCH_SIZE=32
    ports:
      - "0.0.0.0:8002:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - qwen3a3b

  qwen3-32b:
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: qwen3-32b
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=nvidia/Qwen3-32B-NVFP4
      - MAX_BATCH_SIZE=16
    ports:
      - "0.0.0.0:8003:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - qwen332b

  phi4-reasoning:
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: phi4-reasoning
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=nvidia/Phi-4-reasoning-plus-NVFP4
      - MAX_BATCH_SIZE=32
    ports:
      - "0.0.0.0:8004:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - phi4

  llama33-70b:
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: llama33-70b
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=nvidia/Llama-3.3-70B-Instruct-NVFP4
      - MAX_BATCH_SIZE=16
    ports:
      - "0.0.0.0:8005:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - llama3370b

  deepseek-v32:
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: deepseek-v32
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=nvidia/DeepSeek-V3.2-NVFP4
      - MAX_BATCH_SIZE=8
    ports:
      - "0.0.0.0:8006:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - deepseek

  nemotron-120b:
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: nemotron-120b
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4
      - MAX_BATCH_SIZE=8
    ports:
      - "0.0.0.0:8007:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - nemotron120b

  kimi-k25:
    # NOTE: Kimi-K2.5 support requires TRT-LLM v1.3.0rc7+
    # Update image once nvcr.io/nvidia/tensorrt-llm/release:1.3.0rc7 is confirmed available
    image: nvcr.io/nvidia/tensorrt-llm/release:1.2.0rc6
    container_name: kimi-k25
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${HF_TOKEN}
      - MODEL_HANDLE=moonshotai/Kimi-K2.5
      - MAX_BATCH_SIZE=16
    ports:
      - "0.0.0.0:8008:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./config/trtllm-config.yml:/tmp/extra-llm-api-config.yml
      - ./config/start_trtllm.sh:/start_trtllm.sh
    shm_size: '64gb'
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /start_trtllm.sh
    restart: unless-stopped
    profiles:
      - kimi
